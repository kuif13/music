{
  "name": "churchmouse-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev":    "wrangler dev --local",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute churchmouse-db --file=schema.sql",
    "db:local": "wrangler d1 execute churchmouse-db --local --file=schema.sql",
    "pages:deploy": "wrangler pages deploy frontend/ --project-name=churchmouse-music"
  },
  "dependencies": {},
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}
